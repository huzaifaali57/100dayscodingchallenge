var Admin;
var Name;
Name = "John";
Admin = Name;
alert(Admin);

let earth;          // variable with the name of our planet.
let currentVisitor;    // variable to store the name of a current visitor

const birthday = '18.04.1982';
const age = someCode(birthday);  // age is calculated from birthday using someCode()